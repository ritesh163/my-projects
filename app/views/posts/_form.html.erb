<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>

  <div class="field">
    <%#= f.fields_for :uploads ,multipart: true do  |upload|  %>
      <%#= f.label :image %><br>
      <%#= upload.file_field :avatar%>
      <%#= upload.link_to_remove "Remove this task" %>
    <%#=end %>
      <p><%#= f.link_to_add "Add a task", :uploads %></p>
    
    
  </div><br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
